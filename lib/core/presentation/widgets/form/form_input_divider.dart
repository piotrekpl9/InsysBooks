import 'package:flutter/material.dart';

class FormInputDivider extends StatelessWidget {
  const FormInputDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
